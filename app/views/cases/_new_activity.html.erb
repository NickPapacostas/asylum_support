<div class="new-activity-form text-center">
	<h3>Add</h3>

	<%= form_for(@case.activities.build, url: case_activities_path(@case)) do |f| %>

		<div class='form-group'>
			<%= f.label "Type", class: 'form-label' %>
			<%= f.collection_select(:case_activity_type, CaseActivity.types, :to_s, :to_s, {},  {class: 'form-input case-activity-select'}) %>
			<%= f.label "Relevant future date", class: 'form-label' %>
			<%= f.text_field(:relevant_future_datetime, {class: 'form-input case-activity-select', type: "date"}) %>
			<%= f.label "Notes", class: 'form-label' %>
			<%= f.text_area :notes, class: 'form-input activity-notes' %>

		</div>
		<%= f.submit "Create", class: 'btn btn-primary' %>
	<% end %>

</div>